apiVersion: v1
kind: Pod
metadata:
  name: myapp-pod-3
  labels:
    app: myapp
    type: front-end
spec:
  containers: # list of containers
    - name: nginx-container
      image: nginx #docker image
